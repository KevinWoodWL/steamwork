修复
--

*   **汽动输入端 / 输出端直连显示**：修复汽动输入端与汽动输出端、输入端与输入端、输出端与输出端直接相邻连接时，横向管道长度不足、宽度不一致或端头多出一小节的问题
*   **汽动端点连接模型对齐**：直连端点改为与 pylon 货运输入器 / 输出器一致的显示结构，端点自身只绘制短接头，两个端点之间由汽动管道线段补满完整一格连接
*   **汽动直连重复渲染**：直连管道线段只由其中一个端点负责显示，避免两端同时绘制导致模型重叠或厚度异常
