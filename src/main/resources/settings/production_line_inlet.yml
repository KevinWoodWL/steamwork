# 产线入口：耗汽驱动
# 产线每发生一次「物品向下游推进」（入口喂料 / 机器→机器 / 机器→出口，每一跳都算），
# 每推进 1 个物品消耗 steam-per-item 点加压蒸汽；一组堆叠按数量×单价扣。
# 加压蒸汽缓存由蒸汽管道从入口背面（产线走向反方向）接入；缓存不足以支付下一次推进时
# 整条产线停摆，补足加压蒸汽后自动恢复。
steam-per-item: 10.0     # 每个物品每跳消耗的加压蒸汽（mB）
steam-buffer: 10000.0    # 入口加压蒸汽缓存容量（mB）
