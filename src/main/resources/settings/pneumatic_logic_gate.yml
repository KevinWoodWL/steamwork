# 汽动逻辑门：有朝向，比较器式三输入（正后/左/右），正前输出
# 读相邻机器蒸汽压力做逻辑，逻辑成立时把真蒸汽抽进自身缓存供下游传导
tick-interval: 2
transfer-per-tick: 50.0   # 每 tick 每种蒸汽每路最多搬运量（mB）
signal-threshold: 1.0     # 相邻蒸汽量 ≥ 此值算 HIGH 信号
buffer: 1000.0            # 每种蒸汽的内部缓存容量（mB），供下游抽取/串联
